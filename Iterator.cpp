#include "Iterator.h"

// ver si se puede colocar las funciones en este archivo


/*
template<typename T>
Iterator<T>::Iterator()
{
    Nodeit.set_nodeit(NULL);
};


template<typename T>
Iterator<T>::~Iterator()
{
};


template<typename T>
void Iterator<T>::operator=(Node<T>* node){
    Nodeit.set_nodeit(node);
};

template<typename T>
void Iterator<T>::operator ++()
{
    Nodeit = Nodeit->get_next();
}; 

template<typename T>
T Iterator<T>::operator *()
{
    return Nodeit->get_elem();
};

template<typename T>
bool Iterator<T>::operator ==(Node<T>* node)
{
    return Nodeit == node;
};

template<typename T>
bool Iterator<T>::operator !=(Node<T>* node)
{
    return Nodeit != node;
};
*/